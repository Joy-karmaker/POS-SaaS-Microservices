-- Catalog, products, sales, and categories domain tables are provisioned inside each tenant's dedicated database (tenant_<id>).
-- Control plane database only contains platform orchestration tables (tenants, users, auth, stores, shifts, processed_events, audit_logs).